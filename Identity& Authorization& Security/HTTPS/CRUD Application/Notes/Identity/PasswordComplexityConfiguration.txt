what if you wanttoconfigure the complexityof the password that user will login OR regitser with?

Q:where can youconfigurethe password complexity , in program.cs where you are adding your
identity service , create alambda expression 

there youwill find some properties that you can access& modify


            builder.Services.AddIdentity<ApplicationUser, ApplicationRole>(options =>
            {
                options.Password.RequiredLength = 5;
                options.Password.RequireUppercase = false;
                options.Password.RequireNonAlphanumeric = false;
                options.Password.RequireLowercase = true;
                options.Password.RequireDigit = false;
                options.Password.RequiredUniqueChars = 3;
                }
            )