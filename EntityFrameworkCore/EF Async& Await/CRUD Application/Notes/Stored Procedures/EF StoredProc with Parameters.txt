before , we learned how to do a basic proc , like just to return or retrieve some data or rows

,,but what if we need to do a proc for Inserting a person

1-add-migration "Insert Person"

2-      protected override void Up(MigrationBuilder migrationBuilder)
        {
            string sp_InsertPerson = @"CREATE PROCEDURE [dbo].[InsertPerson](@PersonID uniqueidentifier,@PersonName nvarchar(40)
                                      ,@EmailAddress nvarchar(40),@DateOfBirth datetime2(7),
                                       @Gender nvarchar(10),@Address nvarchar(300),@CountryID uniqueidentifier
                                       ,@ReccivenewsLetters bit)
                                      AS BEGIN
                                        INSERT INTO [dbo].[Persons](PersonID,PersonName,EmailAddress,DateOfBirth,
                                       Gender,Address,CountryID,ReccivenewsLetters)
                                        VALUES (@PersonID,@PersonName,@EmailAddress,@DateOfBirth,
                                       @Gender,@Address,@CountryID,@ReccivenewsLetters)
                                      END";

            migrationBuilder.Sql(sp_InsertPerson);
        }


3- dont forget the down method with just DROP instead of CREATE

4- in your DbContext create the method to call the proc

5-in the method , create a new array of SqlParameter type , and its elemets is objects of SqlParameter type -->	new SqlParameter("@PersonID",person.PersonID),
foreach attribute , create a new sql parameter and make it  like this 

6-in the method , we here cant do Persons.FromSqlRaw-->we use this only if retrieving or get some rows 
solution-->Database.ExecuteSqlRaw()

so--> Database.ExecuteSqlRaw("EXEC [dbo].[InsertPerson] @PersonID ,@PersonName,@EmailAddress,@DateOfBirth,@Gender,@Address,@CountryID,@ReccivenewsLetters"
				,sqlParameters);

7-call this method instead of _db.Persons.Add(person) in the add person method in person service