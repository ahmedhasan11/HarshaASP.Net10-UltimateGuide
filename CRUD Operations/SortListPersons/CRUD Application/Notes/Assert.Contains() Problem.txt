Asser.Contains(expected,actual)-->uses the equalsmethod internally to check if the actual contains expected

but the problem is it compares the refenerece-->it compares the obj itself not its values

so wehave to compare the values-->you have to override the equals method in you country response

		public override bool Equals(object? obj)
		{
            if (obj==null)
            {
                return false;
            }

            if (obj.GetType() != typeof(CountryResponse)){ //if theuser havepasseda differenttype thatis not countryresonse
                return false;
            }

            CountryResponse objresponse = (CountryResponse)obj;

            return CountryID == objresponse.CountryID && Countryname == objresponse.Countryname;

			//return base.Equals(obj);
		}

//you dont have to call the equals method explicitly , it will be called automatically while using Assert.Contains(expected, actual)